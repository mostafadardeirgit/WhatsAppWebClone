import { createContext } from "react";

export const LSContentContext = createContext()
export const LSContentProvider = LSContentContext.Provider

export const RSContentContext = createContext()
export const RSContentProvider = RSContentContext.Provider
